# Env var lengkap untuk project ini ada di root: ../../.env.example
# (satu-satunya file authoritative — lihat docs/panduan-deploy-vps.md § 4.2 dan
# docs/lessons-learned.md soal dua .env.example yang sempat drift satu sama lain).
#
# File ini sengaja dibiarkan kosong isinya supaya tidak ada dua sumber kebenaran
# yang bisa saling drift lagi — copy dari root, bukan dari sini.
